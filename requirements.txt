flask>=3.0.0
requests>=2.31.0
gunicorn>=21.0.0
google-genai>=1.0.0
pytest>=8.0.0
stripe>=10.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.0
aiohttp>=3.10.0
pydantic>=2.0.0

# Vercel installs this file for the Python runtime.
# gunicorn is unused on Vercel (WSGI is handled by the platform) but kept for Railway/Docker.
